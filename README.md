# apptainer-examples

Examples of building Apptainer containers for Python, etc.

-   `python-miniconda`: installing a Python with miniconda-based packages
-   `python-pip`: installing a Python with pip-based packages (uses venv)
-   `freesurfer-apptainer`: installing [FreeSurfer](https://github.com/freesurfer/freesurfer) (x86_64 only)
